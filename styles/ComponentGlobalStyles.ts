import { StyleSheet } from 'react-native';

const ComponentGlobalStyles = StyleSheet.create({
    container: {
        display: 'flex',
        height: '100%',
    },
});
export default ComponentGlobalStyles;
