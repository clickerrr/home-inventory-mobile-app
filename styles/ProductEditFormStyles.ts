import { StyleSheet } from 'react-native';

const ProductEditFormStyles = StyleSheet.create({
    container: {
        width: '100%',
        height: 'auto',
        flexDirection: 'column',
        alignItems: 'center',
    },
    item: {
        marginVertical: '2%',
    },
});
export default ProductEditFormStyles;
