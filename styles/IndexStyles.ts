import { StyleSheet } from 'react-native';
const IndexStyles = StyleSheet.create({
    icon: {
        resizeMode: 'contain',
        width: '50%',
        height: '25%',
    },
});
export default IndexStyles;
