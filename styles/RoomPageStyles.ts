import { StyleSheet } from 'react-native';

const RoomPageStyles = StyleSheet.create({
    container: {
        width: '100%',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
    },
});
export default RoomPageStyles;
